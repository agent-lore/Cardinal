# Cardinal

GitHub repository analysis CLI tool.

## Done Criteria (Required)

A change is **not done** unless all checks pass:

1. Lint + format: `make lint`
2. Type check: `make typecheck`
3. Unit tests: `make test`
4. All checks: `make check`
